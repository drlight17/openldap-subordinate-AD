# to force using slapd.conf file instead of slad.d cn=config directory
# !!! DON'T FORGET TO DELETE /etc/ldap/slapd.d FOLDER TO AVOID STARTUP ERROR!!!
# parameter may be empty, if slapd.d is not exist
SLAPD_CONF=/etc/ldap/slapd.conf
